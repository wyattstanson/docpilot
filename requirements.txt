# Core engine runs with the standard library alone. The dependencies below are
# OPTIONAL: the engine transparently falls back to deterministic offline
# behavior when they are absent (mock embeddings, in-memory vector store).

# LLM + embeddings (install the provider you use)
openai>=1.40.0
anthropic>=0.34.0

# Vector store (file-based persistence for embeddings)
chromadb>=0.5.0

# GitHub integration (used by the Action entrypoint)
PyGithub>=2.3.0
